2024.06.13


Input : 입력
Output : 출력

InputStream
OutputStream

Read
Write

보조
BufferedReader
BufferedWriter

*client가 서버에 int 10을 보낸다고 했을 때 중간에 2진수로 이루어진 비트를
기계어로 번역이 됐다가 다시 10으로 변환이 되어 서버에 도달하게 되는데
이게 정수인지 문자열인지 데이터타입이 애매모호하게 된다
그래서 랜선을 넘어오면 전부 String. 문자열 타입으로 받게끔 통일되어있다
문자열은 '문자'를 모은 '배열'로 생각해서
FileReader로 전송받은 비트를 문자로 반환하고 버퍼에 담아두고 나서
다시 그 문자를 BufferedReader로 문자열로 모으는 과정이 필요한 것이다
각 reader는 읽을 준비를 하는 것이라서 이후 readLine으로 한줄씩 읽어나간다
readLine이 한줄씩 읽어나가는 기준은 \n 즉 엔터다

*문자열로 옮기지 못하는 zip같은 압축파일이라던가, 이미지 데이터라던지는
그냥 비트 그대로 옮긴다





